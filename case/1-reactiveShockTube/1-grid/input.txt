// Gridgen(0) or Pointwise(1)
int GridgenOrPointwise = 0

// Dimension
int dimension = 2
// grid file name
string gridFile = shockTube.xyz
// boundary file name
string boundaryFile = shockTube.inp
// number of processors to use
int n_proc = 1
// if the grid is in ASCII(0) or in binary(1) form
bool isBinary = 1
// if we write the grid in ASCII(0) or in binary(1) form
int writeBinary = 1

// If 2D, set all Z values to the given value
bool setZ = 0
real zValue = 0.0